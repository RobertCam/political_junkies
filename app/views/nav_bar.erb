<nav class="navbar navbar-findcond navbar-fixed-top">
    <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Poli Pocket</a>
    </div>
    <div class="collapse navbar-collapse" id="navbar">
      <ul class="nav navbar-nav navbar-right">
      <!-- drop down for repub candidates -->
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-fw fa-bell-o"></i> Republican Candidates <span class="badge">4</span></a>
          <ul class="dropdown-menu" role="menu">
            <% CANDIDATES.each do |name, cand| %>
              <% unless name == :PELOSI || name == :SANDERS %>
                <li><a href="/candidates/show/<%= cand.id %>"><i class="fa fa-fw fa-tag"></i><span class="badge">REP</span><%= cand.name %></a></li>
              <% end %>
            <% end %>
          </ul>
        </li>
          <!-- second drop down for demo candidates -->
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-fw fa-bell-o"></i> Democratic Candidates <span class="badge">2</span></a>
          <ul class="dropdown-menu" role="menu">
            <% CANDIDATES.each do |name, cand| %>
              <% if name == :PELOSI || name == :SANDERS %>
              <li><a href="/candidates/show/<%= cand.id %>"><i class="fa fa-fw fa-tag"></i><span class="badge">DEM</span><%= cand.name %></a></li>
              <% end %>
            <% end %>
          </ul>
        </li>
        <li class="active"><a href="/candidates/compare_candidates">Compare Candidates<span class="sr-only">(current)</span></a></li>
        <li class="active"><a href="https://www.opensecrets.org/" target="_newtab">Open Secrets<span class="sr-only">(current)</span></a></li>
      </ul>
    </div>
  </div>
</nav>